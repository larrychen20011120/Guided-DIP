reproduce:
  seed: 42

image:
  width: 128    # resize to small picture
  height: 128   # resize to small picture

dip:
  backbone_name: "unet"
  num_steps: 500
  optimizer: "Adam"
  learning_rate: 0.001
  device: "cpu"
  num_snapshots: 9

ddpm:
  scheduler: "linear"
  beta_start: 0.0001
  beta_end:  0.02
  num_time_steps: 1000

guide-dip:
  count: 10
  start_t: 500
  end_t: 40
  num_steps: 500
  backbone_name: "unet"
  optimizer: "Adam"
  learning_rate: 0.001
  device: "cpu"
  num_snapshots: 9